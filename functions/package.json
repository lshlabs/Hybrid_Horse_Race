{
  "name": "hybrid-horse-race-functions",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "clean": "rimraf lib",
    "dev": "npm run build -- --watch"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  }
}

